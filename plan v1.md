# Plan v1

## هدف المشروع
إنشاء تطبيق مراقبة شبكة خفيف مع أيقونة عائمة على سطح المكتب تعرض استهلاك الشبكة وتغيّر اللون حسب حالة الاتصال.

## الميزات المطلوبة (مرتبة حسب الأولوية)
1. **أيقونة عائمة على سطح المكتب** – دائرة قابلة للسحب تُظهر استهلاك الشبكة وتغيّر اللون (أحمر = قطع، أخضر = اتصال نشط، رمادي = لا بيانات).  
2. **إشعارات نظام صينية (Tray) مع نقر مزدوج** – فتح النافذة الرئيسية.  
3. **إعدادات تخصيص اللون والحدود** – السماح للمستخدم بتحديد حدود الاستهلاك للون الأحمر/الأخضر.  
4. **تسجيل تاريخي للبيانات** – حفظ إحصاءات الشبكة في ملف SQLite/JSON للعرض لاحقًا.  
5. **واجهة إعدادات داخل التطبيق** – تعديل فترات التحديث، تمكين/تعطيل الإشعارات الصوتية.  
6. **دعم إشعارات صوتية** – تشغيل نغمة عند قطع أو استعادة الاتصال.  
7. **دعم إشعارات بريد/SMS** – إرسال تنبيه عندما يكون الانقطاع أطول من حد معين.  
8. **وضع "Do Not Disturb" وجدولة** – إيقاف الإشعارات في فترات محددة.  
9. **تعدد اللغات** – دعم العربية والإنجليزية على الأقل.  
12. **تحسينات الأداء** – تشغيل المراقبة في خلفية خفيفة (IHostedService) وتقليل استهلاك الذاكرة.

## خطوات التنفيذ (مراحل)
- **المرحلة 1 – أيقونة عائمة**
  - إنشاء نافذة WPF شفافة بدون حدود (`WindowStyle=None`, `AllowsTransparency=True`).
  - ربط `DispatcherTimer` لتحديث استهلاك الشبكة كل 1‑2 ثوانٍ.
  - تغيير لون الدائرة بناءً على القيم (0 KB → أحمر، >0 KB → أخضر، لا اتصال → رمادي).
  - إضافة سحب النافذة عبر `MouseDown` + `DragMove()`.
- **المرحلة 2 – دمج مع الصينية**
  - ربط أيقونة الصينية بـ `TaskbarIcon` وتفعيل `TrayMouseDoubleClick` لفتح النافذة الرئيسية.
- **المرحلة 3 – إعدادات وتخصيص**
  - نافذة Settings لتحديد حدود اللون، فترات التحديث، تمكين الصوت.
- **المرحلة 4 – سجل البيانات**
  - حفظ `NetworkStats` في SQLite أو JSON مع تاريخ/وقت.
- **المرحلة 5 – إشعارات صوتية/بريد**
  - إضافة `System.Media.SoundPlayer` وإعدادات SMTP/Twilio.
- **المرحلة 6 – جدولة Do Not Disturb**
  - إضافة جدول زمني في Settings وتعطيل الإشعارات وفقًا له.
- **المرحلة 7 – تعدد اللغات**
  - ملفات موارد `.resx` للغات.
- **المرحلة 8 – تحسين الأداء**
  - تحويل المراقبة إلى `IHostedService` واستخدام `Channel` للبيانات.

## جدول زمني مبدئي
| الأسبوع | المهمة |
|--------|----------|
| 1‑2 | تطوير الأيقونة العائمة وربطها بالمراقبة. |
| 3   | دمج أيقونة الصينية وتفعيل النقر المزدوج. |
| 4‑5 | إعدادات المستخدم وتخصيص اللون. |
| 6   | سجل البيانات وتخزينه. |
| 7   | إشعارات صوتية وإرسال بريد/SMS. |
| 8   | جدولة Do Not Disturb وتعدد اللغات. |
| 9   | تحسين الأداء والاختبار النهائي. |

## ملاحظات إضافية
- جميع المكونات ستُبنى باستخدام .NET 10 وWPF.
- سيتم إضافة مكتبة `Hardcodet.NotifyIcon.Wpf` للأيقونة الصينية.
- سيُستَخدم `System.Net.NetworkInformation` لجمع إحصاءات الشبكة.

---
*الرجاء مراجعة الخطة وإبلاغي بالموافقة أو أي تعديل مطلوب قبل المتابعة.*
