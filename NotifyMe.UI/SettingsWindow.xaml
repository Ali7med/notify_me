<Window x:Class="NotifyMe.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NotifyMe Settings" Height="450" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <!-- Colors -->
        <SolidColorBrush x:Key="WindowBackground" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="ControlBackground" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="AccentColor" Color="#4CC2FF"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#AAAAAA"/>

        <!-- Slider Style -->
        <Style TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center">
                            <Border Name="TrackBackground" Height="4" CornerRadius="2" Background="{StaticResource ControlBackground}" VerticalAlignment="Center"/>
                            <Canvas VerticalAlignment="Center" Height="4">
                                <Rectangle Name="PART_SelectionRange" Height="4" Fill="{StaticResource AccentColor}" RadiusX="2" RadiusY="2"/>
                            </Canvas>
                            <Track Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Width="16" Height="16" Fill="{StaticResource AccentColor}" Stroke="#1E1E1E" StrokeThickness="2"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="#1E1E1E"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="15,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource WindowBackground}" CornerRadius="10" BorderThickness="1" BorderBrush="#333333">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="*"/>    <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="20,15">
                <TextBlock Text="Settings" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" 
                        Background="Transparent" Foreground="{StaticResource TextSecondary}"
                        Click="CloseButton_Click" Margin="0,-5,-10,0"/>
            </Grid>

            <!-- Content -->
            <StackPanel Grid.Row="1" Margin="20,10">
                <!-- Opacity Section -->
                <TextBlock Text="Appearance" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource AccentColor}" Margin="0,0,0,10"/>
                
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Widget Opacity" Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ElementName=OpacitySlider, Path=Value, StringFormat=P0}" 
                               Foreground="{StaticResource TextSecondary}" Grid.Column="1" VerticalAlignment="Center"/>
                </Grid>
                
                <Slider x:Name="OpacitySlider" Minimum="0.1" Maximum="1.0" Value="0.8" 
                        TickFrequency="0.05" IsSnapToTickEnabled="True" Margin="0,0,0,20"
                        ValueChanged="OpacitySlider_ValueChanged"/>

                <!-- Theme Section -->
                <TextBlock Text="Theme" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,5"/>
                <ComboBox x:Name="ThemeComboBox" Height="35" SelectionChanged="ThemeComboBox_SelectionChanged" Margin="0,0,0,20">
                    <ComboBoxItem Content="Glass (Modern)"/>
                    <ComboBoxItem Content="Classic (Solid)"/>
                </ComboBox>

                <!-- Ping Section -->
                <TextBlock Text="Network" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource AccentColor}" Margin="0,0,0,10"/>
                <TextBlock Text="Ping IP Address" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,5"/>
                <TextBox x:Name="PingHostTextBox" Height="35" Background="{StaticResource ControlBackground}" 
                         Foreground="{StaticResource TextPrimary}" BorderThickness="0" Padding="10,8"
                         VerticalContentAlignment="Center"/>
            </StackPanel>

            <!-- Footer -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20">
                <Button Content="Save Changes" Click="SaveButton_Click" Width="120"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
