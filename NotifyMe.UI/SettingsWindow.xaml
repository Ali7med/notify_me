<Window x:Class="NotifyMe.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="NotifyMe Settings" Height="650" Width="850"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <!-- Color Palette -->
        <SolidColorBrush x:Key="WindowBackground" Color="#121212"/>
        <SolidColorBrush x:Key="SidebarBackground" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="ContentBackground" Color="#121212"/>
        <SolidColorBrush x:Key="CardBackground" Color="#252525"/>
        <SolidColorBrush x:Key="AccentColor" Color="#BB86FC"/>
        <SolidColorBrush x:Key="AccentHoverColor" Color="#9965f4"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#B0B0B0"/>
        <SolidColorBrush x:Key="BorderColor" Color="#333333"/>

        <!-- Typography -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>

        <!-- Navigation ListBoxItem Style -->
        <Style x:Key="NavListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Margin" Value="10,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2C2C2C"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#252525"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom Card Style -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="#000000"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource AccentHoverColor}"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Slider Style -->
        <Style TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center">
                            <Border Name="TrackBackground" Height="4" CornerRadius="2" Background="#333333" VerticalAlignment="Center"/>
                            <Canvas VerticalAlignment="Center" Height="4">
                                <Rectangle Name="PART_SelectionRange" Height="4" Fill="{StaticResource AccentColor}" RadiusX="2" RadiusY="2"/>
                            </Canvas>
                            <Track Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Width="16" Height="16" Fill="{StaticResource AccentColor}"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Border Width="20" Height="20" CornerRadius="4" BorderThickness="2" BorderBrush="{StaticResource AccentColor}" Background="Transparent" Margin="0,0,12,0">
                                <Path x:Name="CheckMark" Data="M 2,10 L 8,16 L 18,4" Stroke="{StaticResource AccentColor}" StrokeThickness="2.5" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Toggle Button Template -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="32" />
                </Grid.ColumnDefinitions>
                <Border x:Name="Border" 
                        Grid.ColumnSpan="2"
                        CornerRadius="6"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}" />
                <Path x:Name="Arrow" 
                      Grid.Column="1" 
                      HorizontalAlignment="Center" 
                      VerticalAlignment="Center"
                      Data="M0,0 L4,4 L8,0"
                      Stroke="{StaticResource TextSecondary}"
                      StrokeThickness="1.5"/>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="true">
                    <Setter TargetName="Border" Property="Background" Value="#333333" />
                    <Setter TargetName="Arrow" Property="Stroke" Value="White" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                          Template="{StaticResource ComboBoxToggleButton}" 
                                          Grid.Column="2" 
                                          Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"/>
                            <ContentPresenter Name="ContentSite"
                                              IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="15,3,23,3"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left" />
                            <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" 
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="#252525"
                                            BorderThickness="1"
                                            BorderBrush="{StaticResource BorderColor}"
                                            CornerRadius="8"
                                            Margin="0,4,0,0">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3" Color="Black"/>
                                        </Border.Effect>
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4"
                                Margin="2">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#353535"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentColor}"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Border Background="{StaticResource WindowBackground}" CornerRadius="16" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border Grid.Column="0" Background="{StaticResource SidebarBackground}" CornerRadius="16,0,0,16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <StackPanel Grid.Row="0" Margin="30,40,30,30">
                        <TextBlock Text="Settings" FontSize="28" FontWeight="Bold"/>
                        <TextBlock Text="NotifyMe" FontSize="14" Foreground="{StaticResource TextSecondary}" Margin="2,4,0,0"/>
                    </StackPanel>

                    <!-- Navigation Menu -->
                    <ListBox x:Name="NavList" Grid.Row="1" Background="Transparent" BorderThickness="0" SelectedIndex="0" SelectionChanged="NavList_SelectionChanged">
                        <ListBoxItem Content="🎨 Appearance" Style="{StaticResource NavListBoxItem}" Tag="Appearance"/>
                        <ListBoxItem Content="🌐 Network" Style="{StaticResource NavListBoxItem}" Tag="Network"/>
                        <ListBoxItem Content="🔔 Notifications" Style="{StaticResource NavListBoxItem}" Tag="Notifications"/>
                        <ListBoxItem Content="⚙️ Advanced" Style="{StaticResource NavListBoxItem}" Tag="Advanced"/>
                    </ListBox>

                    <!-- Save Button -->
                    <StackPanel Grid.Row="2" Margin="20,20,20,30">
                        <Button x:Name="SaveButton" Content="Save Changes" Click="SaveButton_Click" Height="45"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content Area -->
            <Grid Grid.Column="1" Background="{StaticResource ContentBackground}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Top Bar with Close Button -->
                <Grid Grid.Row="0" Margin="30,25,25,10">
                    <TextBlock x:Name="PageTitle" Text="Appearance" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button x:Name="CloseButton" Content="✕" HorizontalAlignment="Right" VerticalAlignment="Center" 
                            Background="Transparent" Foreground="{StaticResource TextSecondary}" FontSize="18" Padding="10" Click="CloseButton_Click"/>
                </Grid>

                <!-- Content Pages -->
                <Grid Grid.Row="1" Margin="30,0,30,30">
                    
                    <!-- Appearance View -->
                    <ScrollViewer x:Name="ViewAppearance" VerticalScrollBarVisibility="Auto" Visibility="Visible">
                        <StackPanel>
                            <!-- Appearance Settings (Combined) -->
                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <!-- Theme Style -->
                                    <TextBlock Text="Theme Style" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>
                                    <ComboBox x:Name="ThemeComboBox" Height="40" FontSize="14" Padding="10,0" SelectionChanged="ThemeComboBox_SelectionChanged">
                                        <ComboBoxItem Content="✨ Glass (Modern)"/>
                                        <ComboBoxItem Content="⬛ Classic (Solid)"/>
                                    </ComboBox>
                                    <TextBlock Text="Select your preferred visual style for the widget." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,8,0,24"/>

                                    <!-- Divider -->
                                    <Border Height="1" Background="#333333" Margin="0,0,0,24"/>

                                    <!-- Widget Transparency -->
                                    <StackPanel x:Name="TransparencyPanel">
                                        <TextBlock Text="Widget Transparency" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>
                                        <Grid Margin="0,0,0,12">
                                            <TextBlock Text="Opacity Level" Foreground="{StaticResource TextSecondary}"/>
                                            <TextBlock Text="{Binding ElementName=OpacitySlider, Path=Value, StringFormat=P0}" HorizontalAlignment="Right" Foreground="{StaticResource AccentColor}" FontWeight="Bold"/>
                                        </Grid>
                                        <Slider x:Name="OpacitySlider" Minimum="0.1" Maximum="1.0" Value="0.8" TickFrequency="0.1" IsSnapToTickEnabled="True" ValueChanged="OpacitySlider_ValueChanged"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- Language -->
                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock Text="Language" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>
                                    <ComboBox x:Name="LanguageComboBox" Height="40" FontSize="14" Padding="10,0">
                                        <!-- Items populated in code-behind -->
                                    </ComboBox>
                                    <TextBlock Text="Select application language." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,12,0,0"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Network View -->
                    <ScrollViewer x:Name="ViewNetwork" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                        <StackPanel>
                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock Text="Connectivity Server" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>
                                    <TextBox x:Name="PingHostTextBox" Height="40" Padding="10" Background="#252525" Foreground="White" BorderThickness="1" BorderBrush="#333333" VerticalContentAlignment="Center"/>
                                    <TextBlock Text="Quick Select:" Foreground="{StaticResource TextSecondary}" Margin="0,16,0,8" FontSize="12"/>
                                    <WrapPanel>
                                        <Button Content="Google" Tag="8.8.8.8" Click="SetPingHost_Click" Margin="0,0,8,8" Padding="12,6" FontSize="12" Background="#333333" Foreground="White"/>
                                        <Button Content="Cloudflare" Tag="1.1.1.1" Click="SetPingHost_Click" Margin="0,0,8,8" Padding="12,6" FontSize="12" Background="#333333" Foreground="White"/>
                                    </WrapPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Notifications View -->
                    <ScrollViewer x:Name="ViewNotifications" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                        <StackPanel>
                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock Text="Alert Preferences" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>
                                    <RadioButton x:Name="ToastNotificationRadio" GroupName="NotifType" Content="Windows Toast Notifications" Margin="0,0,0,12" Checked="ToastNotificationRadio_Checked" Foreground="White" FontSize="14"/>
                                    <RadioButton x:Name="CustomNotificationRadio" GroupName="NotifType" Content="Custom In-App Notifications" Margin="0,0,0,12" Checked="CustomNotificationRadio_Checked" Foreground="White" FontSize="14"/>
                                    
                                    <StackPanel x:Name="CustomPositionPanel" Visibility="Collapsed" Margin="24,0,0,0">
                                        <TextBlock Text="Position" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,8"/>
                                        <ComboBox x:Name="NotificationPositionComboBox" Width="200" HorizontalAlignment="Left" Height="35">
                                            <ComboBoxItem Content="Top Right" Tag="TopRight" IsSelected="True"/>
                                            <ComboBoxItem Content="Top Left" Tag="TopLeft"/>
                                            <ComboBoxItem Content="Bottom Right" Tag="BottomRight"/>
                                            <ComboBoxItem Content="Bottom Left" Tag="BottomLeft"/>
                                        </ComboBox>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock Text="Do Not Disturb" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>
                                    <CheckBox x:Name="DNDCheckBox" Content="Enable Do Not Disturb" Margin="0,0,0,12" Checked="DNDCheckBox_Checked" Unchecked="DNDCheckBox_Checked"/>
                                    
                                    <CheckBox x:Name="DNDScheduleCheckBox" Content="Schedule DND" Margin="24,0,0,8" Checked="DNDScheduleCheckBox_Checked" Unchecked="DNDScheduleCheckBox_Checked"/>
                                    
                                    <StackPanel x:Name="DNDSchedulePanel" Visibility="Collapsed" Margin="48,0,0,0" Orientation="Horizontal">
                                        <TextBox x:Name="DNDStartBox" Width="80" Style="{DynamicResource MaterialDesignTextBox}" MaterialDesign:HintAssist.Hint="Start (HH:mm)" Margin="0,0,16,0"/>
                                        <TextBlock Text="to" VerticalAlignment="Center" Margin="0,0,16,0" Foreground="{StaticResource TextSecondary}"/>
                                        <TextBox x:Name="DNDEndBox" Width="80" Style="{DynamicResource MaterialDesignTextBox}" MaterialDesign:HintAssist.Hint="End (HH:mm)"/>
                                    </StackPanel>
                                    <TextBlock Text="Mutes all sounds and notifications." Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,8,0,0"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock Text="Sound &amp; Feedback" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>
                                    <CheckBox x:Name="ToastNotificationsCheckBox" Content="Show Toast Popups" Margin="0,0,0,12"/>
                                    <CheckBox x:Name="SoundNotificationsCheckBox" Content="Play Sound Alerts"/>
                                    
                                    <!-- Test Buttons -->
                                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                                        <Button Content="Test Notification" Click="TestNotification_Click" Style="{DynamicResource MaterialDesignFlatButton}" Margin="0,0,8,0"/>
                                        <Button Content="Test Sound" Click="TestSound_Click" Style="{DynamicResource MaterialDesignFlatButton}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Advanced View -->
                    <ScrollViewer x:Name="ViewAdvanced" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                        <StackPanel>
                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock Text="Update Frequency" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                    <TextBlock Text="Adjust how often data refreshes." Foreground="{StaticResource TextSecondary}" FontSize="13" Margin="0,0,0,16"/>
                                    
                                    <Grid Margin="0,0,0,12">
                                        <TextBlock Text="Interval" Foreground="{StaticResource TextSecondary}"/>
                                        <TextBlock Text="{Binding ElementName=UpdateIntervalSlider, Path=Value, StringFormat={}{0}s}" HorizontalAlignment="Right" Foreground="{StaticResource AccentColor}" FontWeight="Bold"/>
                                    </Grid>
                                    <Slider x:Name="UpdateIntervalSlider" Minimum="1" Maximum="10" Value="1" TickFrequency="1" IsSnapToTickEnabled="True"/>
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock Text="High Traffic Threshold" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBox x:Name="TrafficThresholdTextBox" Height="40" Padding="10" Background="#252525" Foreground="White" BorderThickness="1" BorderBrush="#333333" VerticalContentAlignment="Center" Margin="0,0,12,0"/>
                                        <ComboBox x:Name="TrafficUnitComboBox" Grid.Column="1" Width="100" Height="40">
                                            <ComboBoxItem Content="KB/s"/>
                                            <ComboBoxItem Content="MB/s" IsSelected="True"/>
                                            <ComboBoxItem Content="GB/s"/>
                                        </ComboBox>
                                    </Grid>
                                </StackPanel>
                            </Border>
                            
                            <Border Style="{StaticResource CardStyle}">
                                <StackPanel>
                                    <TextBlock Text="Application" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,16"/>
                                    <CheckBox x:Name="RunAtStartupCheckBox" Content="Run at Startup"/>
                                    <Button Content="Reset All Settings" HorizontalAlignment="Left" Background="#CF6679" Foreground="White" Margin="0,24,0,0" Padding="16,8" FontSize="12"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
