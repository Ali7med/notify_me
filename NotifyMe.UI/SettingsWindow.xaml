<Window x:Class="NotifyMe.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NotifyMe Settings" Height="600" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <!-- Colors -->
        <SolidColorBrush x:Key="WindowBackground" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="ControlBackground" Color="#2D2D2D"/>
        <SolidColorBrush x:Key="AccentColor" Color="#4CC2FF"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#AAAAAA"/>
        <SolidColorBrush x:Key="BorderColor" Color="#3A3A3A"/>

        <!-- TabControl Style -->
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- TabItem Style -->
        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" Background="{StaticResource ControlBackground}" 
                                BorderThickness="0" CornerRadius="6,6,0,0" Margin="2,0,2,0" Padding="20,12">
                            <ContentPresenter ContentSource="Header" 
                                            HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- Slider Style -->
        <Style TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid VerticalAlignment="Center">
                            <Border Name="TrackBackground" Height="4" CornerRadius="2" 
                                    Background="{StaticResource ControlBackground}" VerticalAlignment="Center"/>
                            <Canvas VerticalAlignment="Center" Height="4">
                                <Rectangle Name="PART_SelectionRange" Height="4" 
                                          Fill="{StaticResource AccentColor}" RadiusX="2" RadiusY="2"/>
                            </Canvas>
                            <Track Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Width="18" Height="18" 
                                                        Fill="{StaticResource AccentColor}" 
                                                        Stroke="#1E1E1E" StrokeThickness="3"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentColor}"/>
            <Setter Property="Foreground" Value="#1E1E1E"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="20,10">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border Width="20" Height="20" 
                                    Background="{StaticResource ControlBackground}" 
                                    BorderBrush="{StaticResource AccentColor}" 
                                    BorderThickness="2" CornerRadius="4" Margin="0,0,10,0">
                                <TextBlock x:Name="CheckMark" Text="✓" 
                                          FontSize="14" FontWeight="Bold"
                                          Foreground="{StaticResource AccentColor}" 
                                          HorizontalAlignment="Center" 
                                          VerticalAlignment="Center" Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource ControlBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="12,10">
                            <Grid>
                                <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Left"/>
                                <ToggleButton Name="ToggleButton" Focusable="False"
                                            IsChecked="{Binding Path=IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                            ClickMode="Press"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="{TemplateBinding Background}">
                                                <TextBlock Text="▼" Foreground="{StaticResource TextSecondary}" 
                                                          HorizontalAlignment="Right" VerticalAlignment="Center" 
                                                          Margin="0,0,10,0" FontSize="10"/>
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <Popup Name="Popup" Placement="Bottom" Focusable="False" AllowsTransparency="True"
                                       IsOpen="{TemplateBinding IsDropDownOpen}" PopupAnimation="Slide">
                                    <Border Background="{StaticResource ControlBackground}" 
                                            BorderBrush="{StaticResource AccentColor}" BorderThickness="1" 
                                            CornerRadius="6" MaxHeight="200">
                                        <ScrollViewer>
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem Style -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Name="Border" Background="{TemplateBinding Background}" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#3A3A3A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource WindowBackground}" CornerRadius="12" 
            BorderThickness="1" BorderBrush="#333333">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.7" Color="#000000"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="*"/>    <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="25,20,25,15">
                <StackPanel>
                    <TextBlock Text="⚙️ Settings" FontSize="24" FontWeight="Bold" 
                              Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="Customize your NotifyMe experience" FontSize="12" 
                              Foreground="{StaticResource TextSecondary}" Margin="0,5,0,0"/>
                </StackPanel>
                <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" 
                        Background="Transparent" Foreground="{StaticResource TextSecondary}"
                        Click="CloseButton_Click" Margin="0,-5,0,0" Padding="10,5"/>
            </Grid>

            <!-- TabControl -->
            <TabControl Grid.Row="1" Margin="20,0,20,0">
                <!-- Appearance Tab -->
                <TabItem Header="🎨 Appearance">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10">
                        <StackPanel Margin="15">
                            <!-- Opacity -->
                            <TextBlock Text="Widget Transparency" FontSize="14" FontWeight="SemiBold" 
                                      Foreground="{StaticResource TextPrimary}" Margin="0,0,0,10"/>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Slider x:Name="OpacitySlider" Minimum="0.1" Maximum="1.0" Value="0.8" 
                                       TickFrequency="0.05" IsSnapToTickEnabled="True"
                                       ValueChanged="OpacitySlider_ValueChanged" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding ElementName=OpacitySlider, Path=Value, StringFormat=P0}" 
                                          Foreground="{StaticResource AccentColor}" Grid.Column="1" 
                                          VerticalAlignment="Center" FontWeight="Bold" FontSize="14" Margin="15,0,0,0"/>
                            </Grid>
                            <TextBlock Text="Adjust how transparent the floating widget appears" 
                                      Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,0,0,25"/>

                            <!-- Theme -->
                            <TextBlock Text="Theme Style" FontSize="14" FontWeight="SemiBold" 
                                      Foreground="{StaticResource TextPrimary}" Margin="0,0,0,10"/>
                            <ComboBox x:Name="ThemeComboBox" Height="40" 
                                     SelectionChanged="ThemeComboBox_SelectionChanged" 
                                     Background="{StaticResource ControlBackground}"
                                     Foreground="{StaticResource TextPrimary}" BorderThickness="0">
                                <ComboBoxItem Content="✨ Glass (Modern &amp; Transparent)"/>
                                <ComboBoxItem Content="⬛ Classic (Solid Background)"/>
                            </ComboBox>
                            <TextBlock Text="Choose your preferred visual style" 
                                      Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,10,0,0"/>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- Network Tab -->
                <TabItem Header="🌐 Network">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10">
                        <StackPanel Margin="15">
                            <!-- Ping Host -->
                            <TextBlock Text="Connectivity Test Server" FontSize="14" FontWeight="SemiBold" 
                                      Foreground="{StaticResource TextPrimary}" Margin="0,0,0,10"/>
                            <TextBox x:Name="PingHostTextBox" Height="40" 
                                    Background="{StaticResource ControlBackground}" 
                                    Foreground="{StaticResource TextPrimary}" 
                                    BorderThickness="1" BorderBrush="{StaticResource BorderColor}"
                                    Padding="12,10" VerticalContentAlignment="Center" FontSize="13"/>
                            <TextBlock Text="IP address used to check internet connectivity (e.g., 8.8.8.8, 1.1.1.1)" 
                                      Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,8,0,25"/>

                            <!-- Common Servers -->
                            <TextBlock Text="Quick Select" FontSize="13" FontWeight="SemiBold" 
                                      Foreground="{StaticResource TextPrimary}" Margin="0,0,0,10"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <Button Content="Google DNS" Click="SetPingHost_Click" Tag="8.8.8.8" 
                                       Margin="0,0,10,0" Padding="15,8" Background="{StaticResource ControlBackground}" 
                                       Foreground="{StaticResource TextPrimary}"/>
                                <Button Content="Cloudflare" Click="SetPingHost_Click" Tag="1.1.1.1" 
                                       Margin="0,0,10,0" Padding="15,8" Background="{StaticResource ControlBackground}" 
                                       Foreground="{StaticResource TextPrimary}"/>
                                <Button Content="OpenDNS" Click="SetPingHost_Click" Tag="208.67.222.222" 
                                       Padding="15,8" Background="{StaticResource ControlBackground}" 
                                       Foreground="{StaticResource TextPrimary}"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- Notifications Tab -->
                <TabItem Header="🔔 Notifications">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10">
                        <StackPanel Margin="15">
                            <TextBlock Text="Alert Preferences" FontSize="14" FontWeight="SemiBold" 
                                      Foreground="{StaticResource TextPrimary}" Margin="0,0,0,15"/>

                            <!-- Notification Type -->
                            <TextBlock Text="Notification Type" FontSize="14" FontWeight="SemiBold" 
                                      Foreground="{StaticResource TextPrimary}" Margin="0,0,0,10"/>
                            <Border Background="{StaticResource ControlBackground}" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                                <StackPanel>
                                    <RadioButton x:Name="ToastNotificationRadio" Content="🪟 Windows Toast Notifications" 
                                                Foreground="{StaticResource TextPrimary}" FontSize="13" Margin="0,0,0,8" 
                                                IsChecked="True" GroupName="NotificationType" Checked="ToastNotificationRadio_Checked"/>
                                    <TextBlock Text="Use built-in Windows notifications (shows in Action Center)" 
                                              Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="30,0,0,15"/>
                                    
                                    <RadioButton x:Name="CustomNotificationRadio" Content="✨ Custom Notifications" 
                                                Foreground="{StaticResource TextPrimary}" FontSize="13" Margin="0,0,0,8"
                                                GroupName="NotificationType" Checked="CustomNotificationRadio_Checked"/>
                                    <TextBlock Text="Use custom notification window with configurable position" 
                                              Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="30,0,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Custom Notification Position (only visible when Custom is selected) -->
                            <Border x:Name="CustomPositionPanel" Visibility="Collapsed" 
                                   Background="{StaticResource ControlBackground}" CornerRadius="8" Padding="15" Margin="0,0,0,15">
                                <StackPanel>
                                    <TextBlock Text="Notification Position" FontSize="13" FontWeight="SemiBold" 
                                              Foreground="{StaticResource TextPrimary}" Margin="0,0,0,10"/>
                                    <ComboBox x:Name="NotificationPositionComboBox" Height="40" FontSize="13">
                                        <ComboBoxItem Content="⬈ Top Right" Tag="TopRight" IsSelected="True"/>
                                        <ComboBoxItem Content="⬉ Top Left" Tag="TopLeft"/>
                                        <ComboBoxItem Content="⬊ Bottom Right" Tag="BottomRight"/>
                                        <ComboBoxItem Content="⬋ Bottom Left" Tag="BottomLeft"/>
                                    </ComboBox>
                                </StackPanel>
                            </Border>

                            <!-- Toast Notifications -->
                            <Border Background="{StaticResource ControlBackground}" 
                                   CornerRadius="8" Padding="15" Margin="0,0,0,15">
                                <StackPanel>
                                    <CheckBox x:Name="ToastNotificationsCheckBox" 
                                             Content="📢 Show Toast Notifications" FontSize="13" Margin="0,0,0,8"/>
                                    <TextBlock Text="Display popup notifications when internet connection changes" 
                                              Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="30,0,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Sound Notifications -->
                            <Border Background="{StaticResource ControlBackground}" 
                                   CornerRadius="8" Padding="15" Margin="0,0,0,15">
                                <StackPanel>
                                    <CheckBox x:Name="SoundNotificationsCheckBox" 
                                             Content="🔊 Play Sound Alerts" FontSize="13" Margin="0,0,0,8"/>
                                    <TextBlock Text="Play audio when connection is lost or restored" 
                                              Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="30,0,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Info -->
                            <Border Background="#2A2A3A" CornerRadius="8" Padding="15" BorderThickness="1" 
                                   BorderBrush="{StaticResource AccentColor}">
                                <StackPanel>
                                    <TextBlock Text="💡 Tip" FontSize="12" FontWeight="Bold" 
                                              Foreground="{StaticResource AccentColor}" Margin="0,0,0,5"/>
                                    <TextBlock TextWrapping="Wrap" Foreground="{StaticResource TextSecondary}" FontSize="11">
                                        You can enable both, one, or neither option based on your preference. 
                                        Toast notifications show detailed info, while sounds provide quick audio feedback.
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- Advanced Tab -->
                <TabItem Header="⚙️ Advanced">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="10">
                        <StackPanel Margin="15">
                            <!-- Update Interval -->
                            <TextBlock Text="Update Frequency" FontSize="14" FontWeight="SemiBold" 
                                      Foreground="{StaticResource TextPrimary}" Margin="0,0,0,10"/>
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Slider x:Name="UpdateIntervalSlider" Minimum="1" Maximum="10" Value="1" 
                                       TickFrequency="1" IsSnapToTickEnabled="True" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding ElementName=UpdateIntervalSlider, Path=Value, StringFormat={}{0:F0} sec}" 
                                          Foreground="{StaticResource AccentColor}" Grid.Column="1" 
                                          VerticalAlignment="Center" FontWeight="Bold" FontSize="14" Margin="15,0,0,0"/>
                            </Grid>
                            <TextBlock Text="How often the widget updates network statistics (lower = more responsive, higher = less CPU usage)" 
                                      Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,0,0,25" TextWrapping="Wrap"/>

                            <!-- Traffic Threshold -->
                            <TextBlock Text="High Traffic Alert Threshold" FontSize="14" FontWeight="SemiBold" 
                                      Foreground="{StaticResource TextPrimary}" Margin="0,0,0,10"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="TrafficThresholdTextBox" Height="40" 
                                        Background="{StaticResource ControlBackground}" 
                                        Foreground="{StaticResource TextPrimary}" 
                                        BorderThickness="1" BorderBrush="{StaticResource BorderColor}"
                                        Padding="12,10" VerticalContentAlignment="Center" FontSize="13"/>
                                <ComboBox x:Name="TrafficUnitComboBox" Grid.Column="1" Height="40" 
                                         Margin="10,0,0,0" FontSize="13">
                                    <ComboBoxItem Content="KB/s"/>
                                    <ComboBoxItem Content="MB/s" IsSelected="True"/>
                                    <ComboBoxItem Content="GB/s"/>
                                </ComboBox>
                            </Grid>
                            <TextBlock Text="Threshold for considering traffic as 'high' (shows alert when exceeded)" 
                                      Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,8,0,25"/>

                            <!-- Auto-Start -->
                            <TextBlock Text="Startup Behavior" FontSize="14" FontWeight="SemiBold" 
                                      Foreground="{StaticResource TextPrimary}" Margin="0,0,0,10"/>
                            <Border Background="{StaticResource ControlBackground}" 
                                   CornerRadius="8" Padding="15" Margin="0,0,0,0">
                                <StackPanel>
                                    <CheckBox x:Name="AutoStartCheckBox" 
                                             Content="🚀 Start with Windows" FontSize="13" Margin="0,0,0,8"/>
                                    <TextBlock Text="Automatically launch NotifyMe when Windows starts" 
                                              Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="30,0,0,0"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>

            <!-- Footer -->
            <Border Grid.Row="2" Background="{StaticResource ControlBackground}" 
                   CornerRadius="0,0,12,12" Padding="25,15">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="💾 Save Changes" Click="SaveButton_Click" Padding="25,12" FontSize="13"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
