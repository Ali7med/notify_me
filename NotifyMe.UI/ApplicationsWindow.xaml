<Window x:Class="NotifyMe.UI.ApplicationsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Applications - Network Monitor" 
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Network Applications" FontSize="28" FontWeight="Bold"/>
            <TextBlock Text="Monitor network usage per application" FontSize="14" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </StackPanel>

        <!-- Search & Filter Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchBox" 
                     materialDesign:HintAssist.Hint="Search applications..."
                     Style="{DynamicResource MaterialDesignOutlinedTextBox}"
                     TextChanged="SearchBox_TextChanged"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10,0,0,0">
                <ComboBox x:Name="SortComboBox" 
                          Width="150"
                          materialDesign:HintAssist.Hint="Sort by"
                          SelectionChanged="SortComboBox_SelectionChanged">
                    <ComboBoxItem Content="Name" Tag="Name"/>
                    <ComboBoxItem Content="Data Usage" Tag="Usage" IsSelected="True"/>
                    <ComboBoxItem Content="Connections" Tag="Connections"/>
                    <ComboBoxItem Content="Last Activity" Tag="Activity"/>
                </ComboBox>
            </StackPanel>
        </Grid>

        <!-- Applications List -->
        <DataGrid x:Name="ApplicationsDataGrid" 
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="{DynamicResource MaterialDesignDivider}">
            <DataGrid.Columns>
                <!-- Application Name with Icon -->
                <DataGridTemplateColumn Header="Application" Width="2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Application" 
                                                       Width="24" Height="24" 
                                                       Margin="0,0,10,0"
                                                       VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding ProcessName}" FontSize="14" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Connections -->
                <DataGridTextColumn Header="Connections" 
                                  Binding="{Binding ConnectionCount}" 
                                  Width="*"/>

                <!-- Data Sent -->
                <DataGridTemplateColumn Header="Sent" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding BytesSentFormatted}" Foreground="#4CAF50"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Data Received -->
                <DataGridTemplateColumn Header="Received" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding BytesReceivedFormatted}" Foreground="#2196F3"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Last Activity -->
                <DataGridTextColumn Header="Last Activity" 
                                  Binding="{Binding LastActivityFormatted}" 
                                  Width="*"/>

                <!-- Category -->
                <DataGridTemplateColumn Header="Category" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource PrimaryHueMidBrush}" 
                                  CornerRadius="3" 
                                  Padding="5,2">
                                <TextBlock Text="{Binding Category}" 
                                         FontSize="10" 
                                         Foreground="White"
                                         HorizontalAlignment="Center"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
