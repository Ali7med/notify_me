<Window x:Class="NotifyMe.UI.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Network History - NotifyMe" 
        Height="650" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Network History" FontSize="28" FontWeight="Bold"/>
            <TextBlock Text="Complete log of your network activity" FontSize="14" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
        </StackPanel>

        <!-- Filters -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <ComboBox x:Name="DateRangeComboBox" 
                     Width="180"
                     materialDesign:HintAssist.Hint="Time Range"
                     SelectionChanged="DateRangeComboBox_SelectionChanged"
                     Style="{DynamicResource MaterialDesignOutlinedComboBox}">
                <ComboBoxItem Content="Last Hour" Tag="LastHour"/>
                <ComboBoxItem Content="Last 24 Hours" Tag="Last24Hours" IsSelected="True"/>
                <ComboBoxItem Content="Last 7 Days" Tag="Last7Days"/>
                <ComboBoxItem Content="Last 30 Days" Tag="Last30Days"/>
                <ComboBoxItem Content="All Time" Tag="AllTime"/>
            </ComboBox>

            <Button x:Name="BtnRefresh" 
                   Content="REFRESH" 
                   Click="RefreshButton_Click" 
                   Margin="15,0,0,0"
                   Style="{DynamicResource MaterialDesignRaisedButton}"/>

            <Button x:Name="BtnExport" 
                   Content="EXPORT CSV" 
                   Click="ExportButton_Click" 
                   Margin="10,0,0,0"
                   Style="{DynamicResource MaterialDesignOutlinedButton}"/>
        </StackPanel>

        <!-- Statistics Summary -->
        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <materialDesign:Card Grid.Column="0" Padding="15" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="TOTAL RECORDS" FontSize="10" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                    <TextBlock x:Name="RecordCountText" Text="0" FontSize="22" FontWeight="Bold" Margin="0,5,0,0"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="1" Padding="15" Margin="10,0,10,0">
                <StackPanel>
                    <TextBlock Text="AVG DOWNLOAD" FontSize="10" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                    <TextBlock x:Name="AvgDownloadText" Text="0 KB/s" FontSize="22" FontWeight="Bold" Margin="0,5,0,0" Foreground="#2196F3"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="2" Padding="15" Margin="10,0,10,0">
                <StackPanel>
                    <TextBlock Text="AVG UPLOAD" FontSize="10" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                    <TextBlock x:Name="AvgUploadText" Text="0 KB/s" FontSize="22" FontWeight="Bold" Margin="0,5,0,0" Foreground="#4CAF50"/>
                </StackPanel>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="3" Padding="15" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="AVG PING" FontSize="10" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                    <TextBlock x:Name="AvgPingText" Text="0 ms" FontSize="22" FontWeight="Bold" Margin="0,5,0,0" Foreground="#FF9800"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>

        <!-- Data Grid -->
        <DataGrid x:Name="HistoryDataGrid" 
                 Grid.Row="3"
                 AutoGenerateColumns="False" 
                 IsReadOnly="True"
                 CanUserAddRows="False"
                 CanUserDeleteRows="False"
                 SelectionMode="Single"
                 GridLinesVisibility="Horizontal"
                 HeadersVisibility="Column"
                 AlternatingRowBackground="{DynamicResource MaterialDesignDivider}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" 
                                  Binding="{Binding Timestamp, StringFormat={}{0:HH:mm:ss}}" 
                                  Width="Auto"/>
                <DataGridTextColumn Header="Date" 
                                  Binding="{Binding Timestamp, StringFormat={}{0:yyyy-MM-dd}}" 
                                  Width="Auto"/>
                <DataGridTemplateColumn Header="Status" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="10" Height="10" 
                                        Fill="{Binding StatusColor}" 
                                        Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Download" 
                                  Binding="{Binding DownloadSpeedFormatted}" 
                                  Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#2196F3"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Upload" 
                                  Binding="{Binding UploadSpeedFormatted}" 
                                  Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#4CAF50"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Ping" 
                                  Binding="{Binding LatencyText}" 
                                  Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#FF9800"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="CLEAR ALL HISTORY" 
                   Click="ClearAllHistory_Click"
                   Margin="0,0,10,0"
                   Background="#D32F2F"
                   Foreground="White"
                   Style="{DynamicResource MaterialDesignRaisedButton}"/>
            
            <Button Content="CLEAR OLD LOGS (30+ DAYS)" 
                   Click="ClearOldLogs_Click"
                   Style="{DynamicResource MaterialDesignFlatButton}"/>
        </StackPanel>
    </Grid>
</Window>
