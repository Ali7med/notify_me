<UserControl x:Class="NotifyMe.UI.Controls.StatCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="250">
    <materialDesign:Card UniformCornerRadius="10" Padding="15" Background="{DynamicResource MaterialDesignPaper}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Title -->
            <TextBlock x:Name="TitleText" Text="TOTAL DOWNLOAD" 
                       Foreground="{DynamicResource MaterialDesignBodyLight}" 
                       FontSize="12" FontWeight="Medium"
                       Grid.Row="0" Grid.Column="0"/>

            <!-- Icon -->
            <materialDesign:PackIcon x:Name="IconControl" Kind="Download" 
                                     Width="24" Height="24" 
                                     Foreground="{DynamicResource PrimaryHueMidBrush}"
                                     Grid.Row="0" Grid.Column="1"/>

            <!-- Value -->
            <TextBlock x:Name="ValueText" Text="12.5 GB" 
                       Foreground="{DynamicResource MaterialDesignBody}" 
                       FontSize="28" FontWeight="Bold"
                       VerticalAlignment="Center"
                       Grid.Row="1" Grid.ColumnSpan="2" Margin="0,10"/>

            <!-- Trend -->
            <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.ColumnSpan="2">
                <materialDesign:PackIcon x:Name="TrendIcon" Kind="ArrowUp" Width="16" Height="16" Foreground="#4CAF50" VerticalAlignment="Center"/>
                <TextBlock x:Name="TrendText" Text="15% vs last week" 
                           Foreground="#4CAF50" 
                           FontSize="12" Margin="5,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</UserControl>
