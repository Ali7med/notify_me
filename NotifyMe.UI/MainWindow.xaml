<Window x:Class="NotifyMe.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NotifyMe.UI"
        mc:Ignorable="d"
        Title="NotifyMe - Network Monitor" 
        Height="500" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock x:Name="HeaderTitle" Grid.Row="0" 
                   Text="Network Monitor" 
                   FontSize="28" 
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Connection Status -->
        <Border Grid.Row="1" 
                Background="#2D2D30" 
                Margin="0,0,0,15">
            <StackPanel>
                <TextBlock x:Name="LblConnectionStatus" Text="Connection Status" 
                          FontSize="16" 
                          FontWeight="SemiBold"
                          Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal">
                    <Ellipse x:Name="StatusIndicator" 
                            Width="16" 
                            Height="16" 
                            Fill="Lime"
                            Margin="0,0,10,0"/>
                    <TextBlock x:Name="StatusText" 
                              Text="Connected" 
                              FontSize="14"
                              VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Traffic Statistics -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Download -->
            <Border Grid.Column="0" Background="#2D2D30" Padding="15" CornerRadius="5">
                <StackPanel>
                    <TextBlock x:Name="LblDownload" Text="Download Speed" FontSize="14" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                    <TextBlock x:Name="DownloadSpeed" Text="0 KB/s" FontSize="24" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBlock x:Name="TotalDownload" Text="Total: 0 B" FontSize="12" Foreground="#808080"/>
                </StackPanel>
            </Border>

            <!-- Upload -->
            <Border Grid.Column="2" Background="#2D2D30" Padding="15" CornerRadius="5">
                <StackPanel>
                    <TextBlock x:Name="LblUpload" Text="Upload Speed" FontSize="14" Foreground="#AAAAAA" Margin="0,0,0,5"/>
                    <TextBlock x:Name="UploadSpeed" Text="0 KB/s" FontSize="24" FontWeight="Bold" Margin="0,0,0,5"/>
                    <TextBlock x:Name="TotalUpload" Text="Total: 0 B" FontSize="12" Foreground="#808080"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Footer -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <TextBlock x:Name="LastUpdate" 
                      Text="Last update: --:--:--" 
                      FontSize="11"
                      Foreground="#808080"
                      VerticalAlignment="Center"
                      Margin="0,0,15,0"/>
            <Button x:Name="BtnMinimize" Content="Minimize to Tray" 
                    Click="MinimizeToTray_Click"
                    Padding="15,8"
                    Background="#007ACC"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="4"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1C97EA"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
