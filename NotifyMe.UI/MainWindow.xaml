<Window x:Class="NotifyMe.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NotifyMe.UI"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        mc:Ignorable="d"
        Title="NotifyMe - Network Monitor" 
        Height="800" Width="1280"
        WindowStartupLocation="CenterScreen"
        Background="#0F0F0F"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Window.Resources>
        <!-- Premium Colors & Gradients -->
        <SolidColorBrush x:Key="SidebarBackground" Color="#161616"/>
        <SolidColorBrush x:Key="CardBackground" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="CardBorder" Color="#333333"/>
        <SolidColorBrush x:Key="AccentColor" Color="#4CC2FF"/>
        <SolidColorBrush x:Key="SecondaryAccent" Color="#BB86FC"/>
        
        <!-- Premium Card Style -->
        <Style x:Key="PremiumCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="4" Opacity="0.3" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Navigation Item Style -->
        <Style x:Key="NavListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="12,4"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Border" CornerRadius="12" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#2D2D2D"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#252525"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Sidebar Button Style -->
        <Style x:Key="SidebarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="12" Padding="16,0">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#252525"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                             <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBackground}" BorderBrush="{StaticResource CardBorder}" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Brand -->
                <StackPanel Grid.Row="0" Margin="32,48,32,48">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="40" Height="40" CornerRadius="12" Background="{StaticResource AccentColor}">
                            <materialDesign:PackIcon Kind="AccessPointNetwork" Width="24" Height="24" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="16,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="NotifyMe" FontSize="20" FontWeight="Bold" Foreground="White" FontFamily="Segoe UI Variable Display"/>
                            <TextBlock Text="PREMIUM" FontSize="10" FontWeight="Bold" Foreground="{StaticResource SecondaryAccent}"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <!-- Navigation -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
                    <StackPanel>
                        <ListBox x:Name="NavList" SelectionChanged="NavList_SelectionChanged" SelectedIndex="0" Background="Transparent" BorderThickness="0" ItemContainerStyle="{StaticResource NavListBoxItem}">
                            
                            <ListBoxItem Tag="Dashboard">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ViewDashboard" Width="24" Height="24" Margin="0,0,16,0"/>
                                    <TextBlock Text="Dashboard" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                </StackPanel>
                            </ListBoxItem>
                            
                            <ListBoxItem Tag="Applications">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Apps" Width="24" Height="24" Margin="0,0,16,0"/>
                                    <TextBlock Text="Applications" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                </StackPanel>
                            </ListBoxItem>
                            
                            <ListBoxItem Tag="Analytics">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ChartBox" Width="24" Height="24" Margin="0,0,16,0"/>
                                    <TextBlock Text="Analytics" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                </StackPanel>
                            </ListBoxItem>
                            
                            <ListBoxItem Tag="History">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="History" Width="24" Height="24" Margin="0,0,16,0"/>
                                    <TextBlock Text="History" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                </StackPanel>
                            </ListBoxItem>
                        </ListBox>
                    </StackPanel>
                </ScrollViewer>

                <!-- Bottom Actions -->
                <StackPanel Grid.Row="2" Margin="24">
                     <Button Style="{StaticResource SidebarButton}" 
                             Click="ShowSettings_Click">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Cog" Width="20" Height="20" Margin="0,0,12,0"/>
                            <TextBlock Text="Settings"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource SidebarButton}" 
                            Foreground="#FF5252"
                            Click="Exit_Click">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Power" Width="20" Height="20" Margin="0,0,12,0"/>
                            <TextBlock Text="Exit App"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="48">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Grid Grid.Row="0" Margin="0,0,0,48">
                    <StackPanel>
                        <TextBlock x:Name="PageTitle" Text="Dashboard" FontSize="32" FontWeight="Bold" Foreground="White" FontFamily="Segoe UI Variable Display"/>
                        <TextBlock Text="Real-time network performance monitor" FontSize="16" Foreground="#888888" Margin="0,8,0,0"/>
                    </StackPanel>
                    
                    <!-- Quick Toggle -->
                    <Border HorizontalAlignment="Right" VerticalAlignment="Top" Padding="12,8" CornerRadius="20" Background="#1A1A1A" BorderBrush="#333333" BorderThickness="1">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Fill="#4CAF50" Margin="0,0,8,0"/>
                            <TextBlock Text="Monitoring Active" Foreground="#4CAF50" FontSize="12" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Stats Cards (Grid Layout) -->
                <Grid Grid.Row="1" Margin="0,0,0,32">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Download -->
                    <Border Grid.Column="0" Style="{StaticResource PremiumCard}">
                        <Grid Margin="24">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Orientation="Horizontal">
                                <Border Width="40" Height="40" CornerRadius="20" Background="#1E2835">
                                    <materialDesign:PackIcon Kind="Download" Foreground="{StaticResource AccentColor}" Width="20" Height="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="DL" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="#888888" FontWeight="Bold"/>
                            </StackPanel>
                            
                             <TextBlock x:Name="DownloadSpeedText" Grid.Row="1" Text="0.0 MB/s" FontSize="26" FontWeight="Bold" Foreground="White" Margin="0,16,0,0"/>
                        </Grid>
                    </Border>
                    
                    <!-- Upload -->
                    <Border Grid.Column="2" Style="{StaticResource PremiumCard}">
                        <Grid Margin="24">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Orientation="Horizontal">
                                <Border Width="40" Height="40" CornerRadius="20" Background="#281E35">
                                    <materialDesign:PackIcon Kind="Upload" Foreground="{StaticResource SecondaryAccent}" Width="20" Height="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="UL" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="#888888" FontWeight="Bold"/>
                            </StackPanel>
                            
                             <TextBlock x:Name="UploadSpeedText" Grid.Row="1" Text="0.0 MB/s" FontSize="26" FontWeight="Bold" Foreground="White" Margin="0,16,0,0"/>
                        </Grid>
                    </Border>
                    
                    <!-- Ping -->
                    <Border Grid.Column="4" Style="{StaticResource PremiumCard}">
                        <Grid Margin="24">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Orientation="Horizontal">
                                <Border Width="40" Height="40" CornerRadius="20" Background="#353520">
                                    <materialDesign:PackIcon Kind="Pulse" Foreground="#FFC107" Width="20" Height="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="PING" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="#888888" FontWeight="Bold"/>
                            </StackPanel>
                            
                             <TextBlock x:Name="LatencyText" Grid.Row="1" Text="24 ms" FontSize="26" FontWeight="Bold" Foreground="White" Margin="0,16,0,0"/>
                        </Grid>
                    </Border>
                    
                     <!-- Signal -->
                    <Border Grid.Column="6" Style="{StaticResource PremiumCard}">
                        <Grid Margin="24">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Orientation="Horizontal">
                                <Border Width="40" Height="40" CornerRadius="20" Background="#203525">
                                    <materialDesign:PackIcon Kind="Wifi" Foreground="#4CAF50" Width="20" Height="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="STATUS" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="#888888" FontWeight="Bold"/>
                            </StackPanel>
                            
                             <TextBlock x:Name="SignalStrengthText" Grid.Row="1" Text="Good" FontSize="20" FontWeight="Bold" Foreground="White" Margin="0,20,0,0"/>
                        </Grid>
                    </Border>

                </Grid>

                <!-- Main Chart Section -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="24"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource PremiumCard}" Height="400">
                        <Grid Margin="24">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="16"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                <TextBlock Text="Traffic Overview" FontSize="18" FontWeight="SemiBold" Foreground="White"/>
                            </StackPanel>
                            
                            <lvc:CartesianChart x:Name="NetworkChart" Grid.Row="2"
                                               Series="{Binding Series}"
                                               XAxes="{Binding XAxes}"
                                               YAxes="{Binding YAxes}"
                                               LegendPosition="Top"
                                               TooltipPosition="Top">
                            </lvc:CartesianChart>
                        </Grid>
                    </Border>

                     <!-- Quick Actions Stack -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Quick Actions" FontSize="18" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,16"/>
                        
                        <Button Style="{DynamicResource MaterialDesignRaisedButton}" Height="60" Margin="0,0,0,16" Background="#252525" BorderBrush="#333333" Foreground="White" materialDesign:ButtonAssist.CornerRadius="12">
                             <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Speedometer" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Run Speed Test" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        
                         <Button Style="{DynamicResource MaterialDesignRaisedButton}" Height="60" Margin="0,0,0,16" Background="#252525" BorderBrush="#333333" Foreground="White" materialDesign:ButtonAssist.CornerRadius="12">
                             <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ChartBoxOutline" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                <TextBlock Text="View Full Report" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        
                         <Button Style="{DynamicResource MaterialDesignRaisedButton}" Height="60" Margin="0,0,0,16" Background="#252525" BorderBrush="#333333" Foreground="White" materialDesign:ButtonAssist.CornerRadius="12">
                             <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Broom" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Clear History" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
